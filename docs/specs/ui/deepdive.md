# UI仕様書 — ステップ3：深掘り分析画面

| 項目 | 内容 |
|---|---|
| 画面名 | 深掘り分析（DeepDiveView） |
| ファイル | src/DeepDiveView.tsx |
| タブ | ステップ3 / 深掘り分析 |
| 作成日 | 2026-04-15 |

---

## 1. 画面概要

deepScore 70以上の銘柄について、web_search + IRBANK データによる詳細分析結果を銘柄カード形式で表示する。  
1度に1枚展開する設計で、複数銘柄を効率よく比較できる。

---

## 2. レイアウト構成

```
┌──────────────────────────────────────────────────────────┐
│ 対象説明バー（deep_score 70以上 / データソース凡例）        │
├──────────────────────────────────────────────────────────┤
│ データ取得日表示（yfinance / IRBANK / web_search）         │
├──────────────────────────────────────────────────────────┤
│ ┌──────────────────────────────────────────────────────┐ │
│ │ 銘柄カード #1（デフォルト展開）                        │ │
│ │ ┌── カードヘッダー ──────────────────────────────┐   │ │
│ │ │ #1  銘柄名（コード）  罠バッジ                  │   │ │
│ │ │     業種 / IRBANK日付 / 鮮度                    │   │ │
│ │ │                  株価  52週高値比  評価バッジ  ▲ │   │ │
│ │ └────────────────────────────────────────────────┘   │ │
│ │ ┌── クイック指標 6項目 ──────────────────────────┐   │ │
│ │ │ PER | PBR | ROE | 配当 | 自己資本比率 | 時価総額 │   │ │
│ │ └────────────────────────────────────────────────┘   │ │
│ │ ┌── 展開コンテンツ（2カラム） ───────────────────┐   │ │
│ │ │ [左] 6項目スコアバー + 直近決算サマリー         │   │ │
│ │ │ [右] プラス材料 + リスク要因 + 本気の評価       │   │ │
│ │ └────────────────────────────────────────────────┘   │ │
│ └──────────────────────────────────────────────────────┘ │
│ ┌── 銘柄カード #2（折りたたみ） ──────────────────────┐ │
│ │ ヘッダーのみ表示                                     │ │
│ └──────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────┘
```

---

## 3. コンポーネント詳細

### 3.1 StockCard（銘柄カード）

#### カードヘッダー

| 要素 | 仕様 |
|---|---|
| ランク番号 | #N 形式、グレー大テキスト |
| 銘柄名 | 大テキスト・太字 |
| コード | （XXXX）形式、emerald色・モノスペース |
| 罠フラグバッジ | normal/suspicious/dangerous バッジ |
| 業種・IRBANK日付・鮮度 | サブテキスト行 |
| 株価 | 大テキスト・太字（右側） |
| 52週高値比 | 「52週高値比 -X.X%」（グレーサブテキスト） |
| 評価バッジ | 強く買い推奨=emerald / 買い推奨=blue / 様子見=amber / 見送り=red |
| 展開インジケータ | ▲（展開中）/ ▼（折りたたみ） |
| クリック | ヘッダー全体クリックで展開/折りたたみ。同時に1枚のみ展開 |

#### クイック指標バー（常時表示）

| 指標 | 表示内容 |
|---|---|
| PER | Xx倍（null の場合「—」） |
| PBR | Xx倍 |
| ROE | Xx% |
| 配当利回 | Xx% |
| 自己資本比率 | Xx% |
| 時価総額 | XXX億 |

背景：bg-gray-50。グリッド6分割。

#### 展開コンテンツ（左カラム）

**6項目スコアバー（ScoreBar コンポーネント）**

| 項目 | 配点 | バー色 |
|---|---|---|
| ① カタリスト | /20 | 達成率≥75%: emerald / ≥50%: blue / 未満: amber |
| ② モメンタム | /20 | 同上 |
| ③ 需給テクニカル | /15 | 同上 |
| ④ バリュエーション | /15 | 同上 |
| ⑤ 下値リスク | /15 | 同上 |
| ⑥ 配当 | /15 | 同上 |

- バーは CSS `width` インラインスタイルで比率表示
- 合計スコア：≥75=emerald / ≥60=blue / 未満=amber

**直近決算サマリー**

| 項目 | 表示 |
|---|---|
| 売上高 前年同期比 | +/-XX.X%（プラス=emerald / マイナス=red） |
| 営業利益 前年同期比 | 同上 |
| 純利益 前年同期比 | 同上 |
| 通期進捗率 | XX%（5年平均 XX%との比較） |
| 修正 | 上方/下方修正テキスト（emerald色） |
| 特別損益 | ⚠ 表示（amber色）。null の場合は非表示 |
| 次回決算予定 | YYYY-MM-DD |

#### 展開コンテンツ（右カラム）

**プラス材料（緑ボックス）**  
`bg-emerald-50 / border-emerald-200`  
箇条書きリスト（• 緑）

**リスク要因（赤ボックス）**  
`bg-red-50 / border-red-200`  
箇条書きリスト（• 赤）

**俺の本気の評価（評価色ボックス）**  
背景色は評価バッジと同系統。評価バッジ + 評価テキスト段落。

---

### 3.2 YoY コンポーネント

| 条件 | スタイル |
|---|---|
| val ≥ 0 | text-emerald-600 / 「+XX.X%」 |
| val < 0 | text-red-600 / 「-XX.X%」 |

### 3.3 評価バッジ定義

| VerdictRating | ラベル | スタイル |
|---|---|---|
| strong_buy | ◎ 強く買い推奨 | bg-emerald-600 / text-white |
| buy | ○ 買い推奨 | bg-blue-600 / text-white |
| watch | △ 様子見 | bg-amber-500 / text-white |
| avoid | ✕ 見送り | bg-red-600 / text-white |

---

## 4. インタラクション

| 操作 | 動作 |
|---|---|
| カードヘッダークリック | カード展開（同インデックスを再クリックで折りたたみ）。同時展開は1枚のみ |
| 初期状態 | インデックス0（1枚目）がデフォルト展開 |

---

## 5. データソース

- `data.ts > deepStocks`（deepScore 70以上の銘柄、現在4件）
