# UI仕様書 — ステップ1：マクロ分析画面

| 項目 | 内容 |
|---|---|
| 画面名 | マクロ分析（MacroView） |
| ファイル | src/MacroView.tsx |
| タブ | ステップ1 / マクロ分析 |
| 作成日 | 2026-04-15 |

---

## 1. 画面概要

3エコノミストのレポートからマクロ環境を読み取り、狙うセクター・避けるセクターを決定する。  
最終的にClaude Code向け実行コマンドを生成してユーザーに提供する。

---

## 2. レイアウト構成

```
┌────────────────────────────────────────────────────────┐
│ [相場天気バッジ]  [相場環境サマリー（追い風/逆風）]        │
├────────────────────────────────────────────────────────┤
│ 3エコノミスト分析                                        │
│ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐       │
│ │ NRI 木内    │ │ 第一生命藤代 │ │ 武者リサーチ │       │
│ │ ・トピック  │ │ ・トピック  │ │ ・トピック  │       │
│ └─────────────┘ └─────────────┘ └─────────────┘       │
├────────────────────────────────────────────────────────┤
│ [狙うセクター（コード付き）]  [避けるセクター]           │
├────────────────────────────────────────────────────────┤
│ Claude Code 実行コマンド                                 │
│ ┌──────────────────────────────────────────────┐       │
│ │ venv\Scripts\python src\pipeline.py ...       │       │
│ └──────────────────────────────────────────────┘       │
│ [コピーボタン]                                          │
└────────────────────────────────────────────────────────┘
```

---

## 3. コンポーネント詳細

### 3.1 相場天気バッジ

| 項目 | 仕様 |
|---|---|
| 表示内容 | 天気アイコン（絵文字）、天気ラベル、「相場の天気」説明テキスト |
| 天気種別 | 晴れ（☀️）/ 曇り（⛅）/ 嵐（⛈️） |
| 色分け | 晴れ=緑系、曇り=黄系、嵐=赤系（背景・ボーダー・テキスト統一） |
| データソース | `data.ts > weather` |

### 3.2 相場環境サマリー

| 項目 | 仕様 |
|---|---|
| 表示内容 | 天気詳細テキスト、追い風ボックス（緑）、逆風ボックス（赤） |
| データソース | `data.ts > weatherDetail` |

### 3.3 3エコノミストカード

| 項目 | 仕様 |
|---|---|
| カード数 | 3枚（NRI木内・第一生命藤代・武者リサーチ）固定 |
| 表示内容 | 氏名、所属機関（エメラルド色）、役職、日付バッジ、トピック箇条書き |
| レイアウト | 3カラムグリッド（md以上）、モバイルは1カラム |
| データソース | `data.ts > economistReports` |

### 3.4 狙うセクターリスト

| 項目 | 仕様 |
|---|---|
| 表示内容 | 業種コードバッジ（緑）、業種名、選定理由テキスト |
| データソース | `data.ts > targetSectors` |

### 3.5 避けるセクターリスト

| 項目 | 仕様 |
|---|---|
| 表示内容 | ✕マーク（赤）、業種名、回避理由テキスト |
| データソース | `data.ts > avoidSectors` |

### 3.6 実行コマンド欄

| 項目 | 仕様 |
|---|---|
| 表示内容 | fetch_stocks.py コマンド文字列（グレー背景・モノスペースフォント・緑テキスト） |
| 絞り込み業種サマリー | コマンド上部に「🎯 現在スクリーニングで絞り込む業種」を業種コード付きチップで表示。`--industries` の値も併記 |
| チップ | プリセット名／「財務: EDINET DB」 |
| コピーボタン | クリックで `navigator.clipboard.writeText()` 実行。2秒間「✓ コピー済み」に変化後復帰 |
| データソース | `data.ts > generatedCommand`（`targetSectors` のコードから `--industries` を自動生成） |

### 3.7 マクロ→業種の自動絞り込み（2026-06-10 追加）

| 項目 | 仕様 |
|---|---|
| 概要 | 「マクロ分析を更新」ボタン押下時、最新記事から **狙う/避ける業種をClaudeが導出** して `data.ts` に書き戻す |
| フロー | `fetch_macro.py`（記事取得）→ `claude -p`（JSON出力：weather/economistReports/**targetSectors**/**avoidSectors**）→ `vite-plugin-macro.ts` が `data.ts` を更新 |
| 業種コード | JPX 33業種コードから厳密選択（プロンプトにコード表を内蔵・4桁数字でバリデーション） |
| 連動 | `targetSectors` 変更 → `generatedCommand` の `--industries` が自動追従 |
| 可視化 | 「狙うセクター（コード付き）」リスト＋コマンド欄上部の絞り込み業種チップで、現在の対象業種が常に見える |
| 実行環境（2026-09-01 追加） | Python の実行コマンドは `vite-plugin-macro.ts` の `PYTHON_BIN` で決まる。**既定は `python3`**（macOS / Linux 標準）。Windows など `python` で起動する環境では `PYTHON=python npm run dev` のように環境変数で上書きする |
| エラー表示 | Python コマンドが見つからない場合（exit=127）は、進捗パネルに「'python3' コマンドが見つかりません。環境変数 PYTHON で実行コマンドを指定してください」と原因と対処を明示する |
| プロンプト受け渡し（2026-09-01 追加） | `CLAUDE_PROMPT` は **stdin 経由**で `claude -p` に渡す。argv に載せると `shell: true` によりシェルへ素通しされ、スキーマ内のバッククォート（```json コードフェンス）がコマンド置換として解釈されプロンプトが壊れる（BUG-002） |

---

## 4. インタラクション

| 操作 | 動作 |
|---|---|
| コピーボタンクリック | コマンド文字列をクリップボードにコピー。ボタンが「✓ コピー済み（緑）」に変化し2秒後に戻る |

---

## 5. 使用カラー

| 要素 | カラークラス |
|---|---|
| 晴れ天気 | bg-emerald-50 / border-emerald-300 / text-emerald-700 |
| 曇り天気 | bg-amber-50 / border-amber-300 / text-amber-700 |
| 嵐天気 | bg-red-50 / border-red-300 / text-red-700 |
| 業種コードバッジ | bg-emerald-50 / text-emerald-700 / border-emerald-200 |
| 避けるセクター | text-red-500 |
| コマンド欄テキスト | text-emerald-700 |
| コピー完了ボタン | bg-emerald-600 / text-white |
